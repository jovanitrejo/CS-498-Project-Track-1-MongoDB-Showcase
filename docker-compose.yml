services:
  backend:
    build:
      context: ./backend
    ports:
      - "8000:8080"
    environment:
      - PORT=8080

  frontend:
    build:
      context: ./frontend
      args:
        VITE_API_BASE_URL: http://localhost:8000
    ports:
      - "5173:8080"
    environment:
      - PORT=8080
    depends_on:
      - backend
